# Premium (private)

This folder is a file-system-synchronized Xcode group. Premium `*.swift` files live in the private repo `alarmed-by-math-premium` and are gitignored here. The app builds without them, falling back to free behavior.
